# home-custom-base
Repository to hold the progress of Roborregos custom base development

Missing:
- Add imu to current working code
- Add bluetooth (through UART2->ESP32)
- Add state machine (config, bluetooth ctrl, normal control [current])

